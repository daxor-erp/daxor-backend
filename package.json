{
	"name": "daxor.api",
	"engines": {
		"node": ">=22"
	},
	"packageManager": "bun@1.2.2",
	"private": true,
	"scripts": {
		"prepare": "husky",
		"lint": "turbo run lint",
		"lint:fix": "turbo run lint:fix",
		"clean": "turbo run clean",
		"format": "turbo run format",
		"build": "dotenv -c -- turbo run build",
		"postinstall": "node scripts/install-chromium.js",
		"install:chrome": "node scripts/install-chromium.js",
		"api:dev": "dotenv -c -- turbo run dev --concurrency 15 --filter=api",
		"api:prod": "dotenv -c -- turbo run prod --concurrency 15 --filter=api",
		"build:dev": "dotenv -e apps/api/.env.development -c -- turbo run build --filter=api",
		"build:prod": "dotenv -e apps/api/.env.production -c -- turbo run build --filter=api"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"dependencies": {
		"nodemailer": "^8.0.2",
		"puppeteer": "^24.15.0"
	},
	"devDependencies": {
		"@repo/biome-config": "workspace:*",
		"@repo/typescript-config": "workspace:*",
		"biome": "^0.3.3",
		"dotenv": "^17.2.3",
		"dotenv-cli": "^8.0.0",
		"husky": "^9.1.7",
		"turbo": "^2.5.4",
		"typescript": "^5.7.3"
	}
}
