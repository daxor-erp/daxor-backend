# Global GraphQL Schema
# Defines base Query and Mutation types that modules extend
# All module schemas use "extend type Query" and "extend type Mutation"

type Query {
	_empty: String
}

type Mutation {
	_empty: String
}
